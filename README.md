Console App - banking system

To have a look at supported commands use command ```help```

Requirements:
* Java v17
* Jackson-core v2.15.2

Json files are used as a database. Default storage in ```db``` directory (contains test data).


